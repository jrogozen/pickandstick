<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header id="header">
      <div class="row">
        <div class="small-6 columns">
          <nav>
            <ul>
              <li><a href="/#/league">League</a></li>
              <li><a href="/#/users">Roster</a></li>
              <li><a href="/#/matchups/new">Add Matchup</a></li>
            </ul>
          </nav>
        </div>
        <div class="user-bar small-6 columns">
          <div id="user_nav">
            <% if current_user %>
              <%= link_to "Hi, #{current_user.username}", "#/users/profile" %>
            <% else %>
              <a href="#/users/login" >Login</a>
            <% end %>
          </div>
        </div>
      </div>
    </header>
  
    <div id="main">
      <%= yield %>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>